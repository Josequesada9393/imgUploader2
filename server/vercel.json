{
    "builds": [
      {
        "src": "index.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.ts" }
    ],
  
      "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
    
  }
  